# Eski .env'i sil
rm .env

# Yeni .env oluştur
cat > .env << 'EOF'
# PostgreSQL - Local Docker
PGHOST=localhost
PGPORT=5433
PGDATABASE=okuloji
PGUSER=okuloji_user
PGPASSWORD=okuloji2024

# Server
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=okuloji_super_secret_key_2025
JWT_EXPIRE=30d
EOF